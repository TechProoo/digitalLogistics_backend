describe('ChatController (placeholder)', () => {
  it('is intentionally a placeholder', () => {
    // This codebase currently has no ChatController.
    // Chat is implemented via WebSocket gateway + ChatService.
    expect(true).toBe(true);
  });
});
