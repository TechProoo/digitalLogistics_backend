services:
  - type: web
    name: digitaldelivery-backend
    runtime: node
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
